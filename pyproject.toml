[project]
name = "snake-5-layer-demo"
version = "0.1.0"
requires-python = ">=3.11"
# No external dependencies — stdlib only

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
